{
  "name": "adesoft-pedidos-mysql",
  "version": "1.0.0",
  "description": "Root package for managing both frontend and backend",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npx expo start\"",
    "start": "npm run dev",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npx expo start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
